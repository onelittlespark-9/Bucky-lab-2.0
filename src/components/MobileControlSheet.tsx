import React,{useState,type ReactNode}from'react';
export function MobileControlSheet({title,primaryAction,children}:{title:string;primaryAction?:ReactNode;children:ReactNode}){const[open,setOpen]=useState(false);return <div className={`mobile-control-sheet ${open?'open':''}`}><div className="sheet-grab" aria-hidden="true"/><div className="sheet-bar"><button type="button" className="sheet-toggle" aria-expanded={open} onClick={()=>setOpen(v=>!v)}>{open?'Hide controls':title}</button>{primaryAction&&<div className="sheet-primary">{primaryAction}</div>}</div><div className="sheet-content">{children}</div></div>}
