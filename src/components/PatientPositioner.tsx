import React,{useEffect,useRef,useState}from'react';import{largestLoadedVolume,type PatientVolume}from'../core/volume';
type Field={x:number;y:number;left:number;right:number;top:number;bottom:number};type Props={volume?:PatientVolume|null;rotation:number;x:number;y:number;beamX?:number;beamY?:number;left?:number;right?:number;top?:number;bottom?:number;onRotation:(v:number)=>void;onBeam?:(x:number,y:number)=>void;onField?:(v:{left:number;right:number;top:number;bottom:number})=>void};
function anatomyProjection(canvas:HTMLCanvasElement,v:PatientVolume,rotation:number){const[nx,ny,nz]=v.manifest.dimensions,w=Math.min(320,nx),h=Math.min(640,nz),ctx=canvas.getContext('2d');if(!ctx)return;canvas.width=w;canvas.height=h;const image=ctx.createImageData(w,h),a=image.data,rad=rotation*Math.PI/180,c=Math.cos(rad),s=Math.sin(rad),cx=(nx-1)/2,cy=(ny-1)/2;for(let py=0;py<h;py++){const z=Math.min(nz-1,Math.round((1-py/(h-1))*(nz-1)));for(let px=0;px<w;px++){const lateral=(px/(w-1)-.5)*nx;let skin=0,bone=0,soft=0;for(let d=-ny*.52;d<=ny*.52;d+=Math.max(1,ny/96)){const x=Math.round(cx+lateral*c-d*s),y=Math.round(cy+lateral*s+d*c);if(x<0||y<0||x>=nx||y>=ny)continue;const hu=v.hu[(z*ny+y)*nx+x];if(hu>-650)skin++;if(hu>-100)soft++;if(hu>250)bone++}const i=(py*w+px)*4;if(!skin){a[i+3]=0;continue}const density=Math.min(1,.18+soft*.018+bone*.055);a[i]=Math.min(255,95+bone*5);a[i+1]=Math.min(255,145+bone*4);a[i+2]=Math.min(255,165+bone*3);a[i+3]=Math.round(90+density*150)}}ctx.putImageData(image,0,0)}
export function PatientPositioner({volume,rotation,x,y,beamX=0,beamY=0,left=28,right=28,top=18,bottom=18,onRotation,onBeam,onField}:Props){const stage=useRef<HTMLDivElement>(null),anatomy=useRef<HTMLCanvasElement>(null),drag=useRef<{mode:'move'|'resize';sx:number;sy:number;f:Field}|null>(null),[local,setLocal]=useState<Field>({x:beamX,y:beamY,left,right,top,bottom}),[loaded,setLoaded]=useState<PatientVolume|null>(volume??largestLoadedVolume());const f=onBeam||onField?{x:beamX,y:beamY,left,right,top,bottom}:local,width=Math.max(10,100-f.left-f.right),height=Math.max(10,100-f.top-f.bottom),fx=f.left+width/2+f.x,fy=f.top+height/2+f.y;useEffect(()=>{if(volume)setLoaded(volume);if(!volume&&!loaded){const id=setInterval(()=>{const v=largestLoadedVolume();if(v)setLoaded(v)},150);return()=>clearInterval(id)}},[volume,loaded]);useEffect(()=>{if(loaded&&anatomy.current)anatomyProjection(anatomy.current,loaded,rotation)},[loaded,rotation]);
function start(e:React.PointerEvent,mode:'move'|'resize'){e.preventDefault();e.currentTarget.setPointerCapture(e.pointerId);drag.current={mode,sx:e.clientX,sy:e.clientY,f:{...f}}}function move(e:React.PointerEvent){const d=drag.current,r=stage.current?.getBoundingClientRect();if(!d||!r)return;const dx=(e.clientX-d.sx)/r.width*100,dy=(e.clientY-d.sy)/r.height*100;if(d.mode==='move'){const nx=Math.max(-45,Math.min(45,d.f.x+dx)),ny=Math.max(-45,Math.min(45,d.f.y+dy));onBeam?onBeam(nx,ny):setLocal(v=>({...v,x:nx,y:ny}))}else{const w=Math.max(8,Math.min(92,(100-d.f.left-d.f.right)+dx*2)),h=Math.max(8,Math.min(92,(100-d.f.top-d.f.bottom)+dy*2)),nf={left:(100-w)/2,right:(100-w)/2,top:(100-h)/2,bottom:(100-h)/2};onField?onField(nf):setLocal(v=>({...v,...nf}))}}
return <div className="patient-positioner" ref={stage}><div className="anatomy-patient" style={{transform:`translate(${x*.7}%,${-y*.7}%)`}}>{loaded?<canvas ref={anatomy} aria-label="Volume-derived anatomical patient"/>:<div className="anatomy-loading">Loading patient anatomy…</div>}</div><div className="light-field" style={{left:`${fx-width/2}%`,top:`${fy-height/2}%`,width:`${width}%`,height:`${height}%`}} onPointerDown={e=>start(e,'move')} onPointerMove={move} onPointerUp={()=>drag.current=null} onPointerCancel={()=>drag.current=null}><span className="field-centre"/><span className="field-label">LIGHT FIELD</span><button className="field-handle" aria-label="Resize collimation" onPointerDown={e=>{e.stopPropagation();start(e,'resize')}} onPointerMove={move} onPointerUp={()=>drag.current=null}/></div><label>Patient rotation <b>{rotation}°</b><input type="range" min="-180" max="180" value={rotation} onChange={e=>onRotation(+e.target.value)}/></label><small>Patient view is generated from loaded HU anatomy. Drag the light field independently to centre; drag the corner to collimate.</small></div>}
