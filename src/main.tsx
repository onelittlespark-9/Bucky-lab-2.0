import React,{useState}from'react';import{createRoot}from'react-dom/client';import'./styles.css';
type View='home'|'xray'|'ct'|'test';
function App(){const[view,setView]=useState<View>('home');const[kvp,setKvp]=useState(110),[mas,setMas]=useState(2),[sid,setSid]=useState(180),[rotation,setRotation]=useState(0),[field,setField]=useState(84);return <main><header><strong onClick={()=>setView('home')}>Bucky Lab 2.0</strong><nav><button onClick={()=>setView('xray')}>X-ray</button><button onClick={()=>setView('ct')}>CT</button><button onClick={()=>setView('test')}>Test</button></nav></header>{view==='home'&&<section><p className="eyebrow">EDUCATIONAL IMAGING SIMULATOR</p><h1>Position. Collimate. Expose. Review.</h1><p>Radiography and CT share one volumetric HU anatomy pipeline. Cases may use real, derived-from-real or high-fidelity synthetic anatomy with explicit provenance.</p><div className="cards"><article><h2>Radiography</h2><p>Practise patient positioning, tube and detector geometry, light-field collimation and exposure selection.</p><button onClick={()=>setView('xray')}>Open X-ray workspace</button></article><article><h2>Advanced CT</h2><p>Axial HU viewing, clinical windowing and protocol teaching from the same teaching volume.</p><button onClick={()=>setView('ct')}>Open CT workspace</button></article></div></section>}{view==='xray'&&<section className="workspace"><div><p className="eyebrow">RADIOGRAPHY</p><h1>X-ray room</h1><div className="imaging-stage"><div className="detector">DETECTOR</div><div className="light-field" style={{inset:`${(100-field)/2}%`}}><span>LIGHT FIELD</span></div><div className="missing">Teaching volume preparation in progress<br/><small>Real and high-fidelity synthetic cases use the same HU projection pipeline.</small></div></div></div><aside><h2>Position</h2><label>Patient rotation <b>{rotation}°</b><input type="range" min="-45" max="45" value={rotation} onChange={e=>setRotation(+e.target.value)}/></label><h2>Collimation</h2><label>Field <b>{field}%</b><input type="range" min="20" max="100" value={field} onChange={e=>setField(+e.target.value)}/></label><h2>Exposure</h2><label>kVp <b>{kvp}</b><input type="range" min="40" max="150" value={kvp} onChange={e=>setKvp(+e.target.value)}/></label><label>mAs <b>{mas.toFixed(1)}</b><input type="range" min="1" max="20" step=".5" value={mas} onChange={e=>setMas(+e.target.value)}/></label><label>SID <b>{sid} cm</b><input type="range" min="90" max="200" value={sid} onChange={e=>setSid(+e.target.value)}/></label><button disabled>Expose — volume required</button></aside></section>}{view==='ct'&&<section><p className="eyebrow">ADVANCED CT</p><h1>CT viewer</h1><div className="ct-grid"><div className="ct-screen"><div className="missing">Teaching volume preparation in progress<br/><small>Axial HU rendering and clinical windowing engine are ready for deployed case data.</small></div></div><article><h2>Protocol families</h2><p>Head · CAP · Colonography · Major Trauma · CTPA · Aorta · KUB</p><h2>Windowing</h2><p>HU-native soft tissue, lung, bone and brain windows operate on volumetric data rather than screenshots.</p><h2>Provenance</h2><p>Every case identifies whether its anatomy is real source, derived from real source, or Bucky Lab synthetic.</p></article></div></section>}{view==='test'&&<section><p className="eyebrow">LEARNING & ASSESSMENT</p><h1>Test area</h1><div className="cards"><article><h2>Radiography assessment</h2><p>Positioning, centring, collimation, SID and exposure quality scoring.</p></article><article><h2>CT assessment</h2><p>Protocol selection, phase recognition, windowing and anatomy/pathology questions.</p></article></div></section>}</main>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
